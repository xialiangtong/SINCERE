Problem
build a CLI tool like Unix tail

MVP 
given a file, print the last 10 lines to stdout.

required flag
support -n <N> to print the last N lines instead of 10

leverage OOP design to make the code like prod level